/**
 * @module
 *
 * Entry point from which you should import all public design (styles).
 *
 * Note: Due to the restrictions of the tailwind configuration, only commonjs module format is supported.
 * Once the tailwind configuration is updated to support esm, this file can be updated to use esm.
 */
module.exports = require('./src/design/index.cjs');
