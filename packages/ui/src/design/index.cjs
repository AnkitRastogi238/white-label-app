// internal dependencies
const colors = require('./colors.cjs');
const screens = require('./screen.cjs');
const typography = require('./typography.cjs');

// expose the public APIs of the design package
module.exports = {
  colors,
  screens,
  typography,
};
